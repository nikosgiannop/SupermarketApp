<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Toolbar ή τίτλος (προαιρετικό) -->
    <TextView
        android:id="@+id/textViewWishlistTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/wishlist_title"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:padding="8dp"/>

    <!-- RecyclerView για εμφάνιση προϊόντων στη wishlist -->

    <!-- Κουμπί εκκαθάρισης wishlist στο κάτω μέρος -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewWishlist"
        android:layout_width="401dp"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="4dp" />

    <Button
        android:id="@+id/buttonClearWishlist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/clear_wishlist"
        android:layout_marginTop="8dp"/>
</LinearLayout>